export type Tier = {
  name: string;
  price: string;
  bullets: string[];
  timeline: string;
  href?: string;
  featured?: boolean;
};
export type CarePlan = {
  name: string;
  price: string;
  features: string[];
};
